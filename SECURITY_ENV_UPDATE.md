# 【重要】.env 更新手順 — Gemini/Google APIキーのサーバー側化対応

## 背景

Gemini / Google Custom Search の APIキーが、ビルド時にフロントエンドのJSバンドルへ平文で焼き込まれ、ブラウザから誰でも閲覧できる状態になっていました。これを修正し、キーは**サーバー側（`/api/gemini-proxy`）でのみ保持**する設計に変更しました。あわせて漏洩したGeminiキーをローテーション（無効化・再発行）し、モデル名も更新しています。

対象は3プロジェクト共通です（apaman / zeenb / factory）。

## 各メンバーが行う作業

### 1. 最新コードを取得

```bash
git pull
npm install   # 依存は変わっていないが念のため
```

コード（プロキシ・SDKシム・モデル名・設定）は git 管理なので pull で自動反映されます。

### 2. .env を手動で更新（← ここが手動必須）

`.env` は `.gitignore` 対象のため **git では同期されません**。各自のPCで手動編集します。

#### 2-1. .env ファイルの開き方

`.env` はドット（`.`）で始まる隠しファイルです。**VS Code などのコードエディタで開くのが確実**です（メモ帳だと拡張子が変わる事故が起きやすい）。

**Mac の場合**

- VS Code: プロジェクトフォルダを VS Code で開くと `.env` が一覧に出ます。ターミナルからは `code .env` でも開けます。
- Finder で見えないとき: プロジェクトフォルダを Finder で開き、`Cmd + Shift + .`（ピリオド）で隠しファイルを表示 → `.env` をエディタにドラッグ。
- ターミナル派: `nano .env`（保存は `Ctrl + O` → `Enter`、終了は `Ctrl + X`）。

**Windows の場合**

- VS Code: プロジェクトフォルダを VS Code で開くと `.env` が出ます。PowerShell からは `code .env`。
- エクスプローラーで見えないとき: 「表示」→「隠しファイル」にチェックを入れる。
- ⚠️ メモ帳で開く場合の注意: そのまま保存すると `.env.txt` になってしまうことがあります。「名前を付けて保存」→「ファイルの種類」を「すべてのファイル」にして、必ず `.env`（拡張子なし）で保存してください。可能なら VS Code を推奨。

#### 2-2. 次の2点を修正

**(A) `GEMINI_API_KEY` を新しいキー値に更新**

```
GEMINI_API_KEY=（管理者から共有された新しいキー）
```

- 旧キーは無効化済みのため、更新しないとGeminiが動作しません。
- 新しいキーの値は Slack 等の安全な経路で管理者から受け取ってください（本手順書には記載しません）。

**(B) 次の行が残っていれば削除**

```
VITE_GEMINI_API_KEY=...   ← 行ごと削除
VITE_GOOGLE_API_KEY=...   ← 行ごと削除
```

- ⚠️ この行を残すと、Vite が `.env` から値を読み取り**再びブラウザバンドルへ焼き込みます**（露出が再発）。コードを最新にするだけでは不十分で、`.env` からの削除までがセットです。
- `GOOGLE_API_KEY`（VITEなし）はそのまま維持でOKです。

### 3. 動作確認

1. サーバー起動（例: `bash ./start.sh`）
2. 記事生成を1回実行し、競合調査・構成チェックなど Gemini を使う工程がエラーにならないこと

## やってはいけないこと（絶対厳守）

- **APIキーに `VITE_` を付けない**（`VITE_` 付きはブラウザJSに平文で埋め込まれる）
- `.env` を git にコミットしない（`.gitignore` 済み）

## 未対応の既知課題（管理者向けメモ）

- Anthropic / OpenAI / Serper のキーは、まだ `VITE_` 版が `.env` に存在しブラウザ露出が残っています。別途ローテーション＋サーバー側化が必要です（特に Anthropic は高額課金リスクのため優先度：高）。
